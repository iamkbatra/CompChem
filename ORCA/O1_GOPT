Steps for electronic structure calculation
- Create an input as .inp (see attached file)
- Create a run as pbs or slurm (see attached file, orca.example)
- Submit the run as "qsub orca.example" for pbs / "sbatch orca.example" for slurm
- Generate an output as .out (see attached file)
